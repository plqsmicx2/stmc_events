# helper function that creates & sets scoreboards

# return to hub if carrot on a stick is used
scoreboard objectives add arena.return used:carrot_on_a_stick

scoreboard objectives remove arena.sidebar